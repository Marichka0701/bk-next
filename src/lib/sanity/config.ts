import { APP_CONFIG } from "@/src/config/appConfig";

export const sanityConfig = {
  projectId: APP_CONFIG.SANITY_PROJECT_ID,
  dataset: APP_CONFIG.SANITY_DATASET,
  apiVersion: "2025-12-20",
  useCdn: APP_CONFIG.SANITY_USE_CDN, 
};
